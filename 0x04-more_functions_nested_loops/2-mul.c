#include<stdio.h>
/**
 * mul(int a, int b) - function multiply the nums
 *
 * @a: an integer
 * @b: the second intger
 *
 * Return: the value of the result
 */
int mul(int a, int b)
{
	return (a * b);
}
